<UserControl x:Class="ChordListenerCS.ChordControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="180" d:DesignWidth="140">
    <Border Background="#2E2E2E" CornerRadius="8" BorderBrush="#444" BorderThickness="1" Margin="5" Padding="10">
        <StackPanel>
            <!-- Chord Name -->
            <TextBlock x:Name="TxtChordName" Text="C Major" FontWeight="Bold" Foreground="White" 
                       HorizontalAlignment="Center" FontSize="16" Margin="0,0,0,10"/>
            
            <!-- Fretboard Area -->
            <!-- 6 Strings, 5 Frets. approx 100x120 -->
            <Canvas x:Name="FretCanvas" Width="100" Height="110" HorizontalAlignment="Center">
                <!-- Will be drawn by code-behind for flexibility -->
            </Canvas>
        </StackPanel>
    </Border>
</UserControl>
